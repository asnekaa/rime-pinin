patch:
  # 开关
  # reset: 默认状态。注释掉后，切换窗口时不会重置到默认状态。
  # states: 方案选单显示的名称。可以注释掉，仍可以通过快捷键切换。
  # abbrev: 默认的缩写取 states 的第一个字符，abbrev 可自定义一个字符
  switches:
    - name: ascii_mode
      states: [中, Ａ]
    - name: ascii_punct # 中英标点
      states: [¥, $]
    - name: traditionalization
      states: [简, 繁]
    - name: full_shape
      states: [半角, 全角]
    - name: search_single_char # search.lua 的功能开关，辅码查词时是否单字优先
      states: [正常, 单字]
      abbrev: [词, 单]

  # 主翻译器，拼音
  # preedit_format 影响到输入框的显示和“Shift+回车”上屏的字符
  translator/preedit_format:
    - xform/([jqxy])[uv]/$1ü/ # 显示为 jü qü xü yü
    - xform/([nl])v/$1ü/ # 显示为 nü lü
    - xform/([nl])[uv]e/$1üe/ # 显示为 nüe lüe
    - xform/(?<=[A-Z])\s(?=[A-Z])// # 消除大写字母之间的空格

  # 自定义短语：custom_phrase/rime-ice.txt
  custom_phrase/user_dict: custom_phrase/rime-ice

  # Emoji
  emoji: null

  # 拼写设定
  speller/algebra/@before 0: "derive/^([jqx])i(a|e|an|ang|ao|ai|en|eng|ong|ing|ian|iang|iao|ie|in|iong|ou|ui|un|uan|ue)$/$1$2/"
